### λ calculus syntax

```
<expression> ::= <name> | <function> | <application>

<name> ::= non-blank chars sequence

<function> ::= λ<name>.<body>

<body> ::= <expression>

<application> ::= (<function expression> <argument expression>)

<function expression> ::= <expression>

<argument expression> ::= <expression>
```

### 2.1

```
λa.(a λb.(a b))
  <function> - λa.(a λb.(a b))
    <bound variable> - a
    <body expression> - (a λb.(a b))
      <application> - (a λb.(a b))
        <function expression> - a
          <name> - a
        <argument expression> - λb.(a b)
          <function> - λb.(a b)
            <bound variable> - b
            <body expression> - (a b)
              <application> - (a b)
                <function expression> - a
                  <name> b
                <argument expression> - b
                  <name> a
```