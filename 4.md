### Recursion and arithmetic

```
def add_recur f x y =
    if is_zero y
    then x
    else f f (succ x) (pred y)

def add = add_recur add_recur
```

```
add one two ==
add_recur add_recur one two ==
(λf.λx.λy
    if is_zero y
    then x
    else f f (succ x) (pred y)) add_recur one two =>
if is_zero two
    then one
    else add_recur add_recur (succ one) (pred two) => 
(λf.λx.λy
    if is_zero y
    then x
    else f f (succ x) (pred y)) add_recur (succ one) (pred two) => ...
if is_zero (pred two)
    then (succ one)
    else add_recur add_recur (succ (succ one)) (pred (pred two)) => ...
(λf.λx.λy
    if is_zero y
    then x
    else f f (succ x) (pred y)) add_recur (succ (succ one)) (pred (pred two)) => ...
if is_zero (pred (pred two))
    then (succ (succ one))
    else (succ (succ (succ one))) (pred (pred (pred two))) => ...
(succ (succ one)) ==
three
```